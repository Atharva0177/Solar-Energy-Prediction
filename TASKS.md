# TASKS.md

Task tracker for the UNISOLAR Solar Power Generation Prediction Platform.
Updated after every completed implementation unit (PRD Rule 11).

**Status legend:** `[ ]` pending · `[~]` in progress · `[x]` done · `[-]` blocked/skipped

A task is DONE only when code exists, tests pass where applicable, expected artifacts exist,
and results are documented (PRD §59). No placeholder implementations count as done.

---

## Phase 0 — Repository Initialization

- [x] Create README.md
- [x] PRD.md present (pre-existing)
- [x] Create TASKS.md
- [x] Create DECISIONS.md
- [x] Create RESULTS.md
- [x] Create directory skeleton per PRD §42
- [x] Add .gitignore
- [x] Write pyproject.toml and requirements.txt
- [x] Create conda environment `solar` (Python 3.13.15) — supersedes initial .venv approach (D-002)
- [x] Install dependencies into conda env `solar`
- [x] Verify importability of core packages — all packages import OK; torch 2.13.0+cu132 with CUDA available on RTX 5070 (capability sm_120), verified 2026-08-24 (D-005 revised)

## Phase 1 — Dataset Acquisition & Inspection

- [x] Dataset files present in `unisolar/` (Solar_Energy_Generation.csv, Weather_Data_reordered_all.csv, Solar_Site_Details.csv, Monthly_Summary_Solar.csv)
- [x] Implement `scripts/inspect_dataset.py` (PRD §6: locate files, shapes, columns, dtypes, head/tail, timestamp/site/target detection, missing %, sampling frequency, duplicates, profile output)
- [x] Generate `artifacts/data_profile.json`
- [x] Generate `artifacts/data_profile.md`
- [x] Verify schema: file names, formats, record counts, site count, sampling interval, timestamp format, target/weather/irradiance columns, units, license terms (PRD §5.1) — verified facts in D-006; **license terms NOT FOUND locally, user to confirm provenance**
- [x] Record verified schema in DECISIONS.md before any downstream ML work (D-006)

## Phase 2 — Data Engineering

- [x] Schema mapping to canonical model (PRD §7) — `src/data/schema_map.py`
- [x] Validation: missing timestamps/gaps, duplicate (site_id, timestamp), impossible values (PRD §9) — 52,492 missing gen slots; weather grid perfect; zero dup keys; zero impossible values (`artifacts/validation_report.md`)
- [x] Cleaning with full operation logging; no blind row deletion — `artifacts/cleaning_log.json`
- [x] Timestamp normalization, site normalization — parsed to datetime64; canonical ids; tz decision D-007
- [x] Missing-value handling strategy — D-008 (power never imputed; weather time-interp ≤2 steps; wind_direction untouched)
- [x] Outlier detection & analysis — IQR fence per site, daylight-only: **0 outliers** flagged, analysis-only (D-009)
- [x] Night-period identification via solar elevation (no irradiance exists, D-006) — pvlib at campus coords, `is_daylight` + `solar_elevation_deg` columns
- [x] Parquet conversion partitioned by site_id/year/month (PRD §41) — 976 files under `data/processed/solar/`, idempotent rebuild
- [x] Deliverable: `data/processed/` — 2,731,946 rows × 14 cols + `site_details.parquet`

## Phase 3 — EDA

- [x] Generation distributions, daily profiles, monthly profiles — `01`–`04`
- [x] Site comparisons, weather correlations — `05`, `06`
- [x] Missingness plots, time-series plots — `07`, `08`, `09`
- [x] Deliverable: `artifacts/eda/` — 9 figures + `eda_summary.md`, generated by `scripts/run_eda.py`

## Phase 4 — Baselines

- [x] Zero baseline — `src/models/baseline.py::ZeroBaseline`
- [x] Mean baseline — global + per-site variants (`MeanBaseline`, D-010)
- [x] Persistence baseline (primary — same-time-previous-day) — `PersistenceBaseline`, causal t−24h lookup, NaN when prior missing
- [x] Deliverable: `baseline_metrics.csv` — `artifacts/baselines/baseline_metrics.csv` (344 rows: 4 baselines × val/test × ALL + 42 sites) + `baselines_report.md`; protocol D-011; 31 tests green

## Phase 5 — Feature Engineering

- [x] Temporal features incl. cyclical encodings (PRD §15) — `src/features/temporal.py`, S-hemisphere seasons
- [x] Lag features, frequency-appropriate (PRD §16) — `src/features/lag.py`, calendar-exact per-site t−Δt (steps 1–96 × 15-min), NaN when prior missing
- [x] Rolling features — history only, never future (PRD §17) — `src/features/rolling.py`, closed-left `[t−W, t)`, windows 1h/6h/24h × mean/std/min/max, per site
- [x] Weather features, dynamic to available variables (PRD §19) — `src/features/weather.py`, 6 vars pass through + circular wind-direction sin/cos
- [x] Solar-position features: optional if lat/lon unavailable (PRD §18) — `src/features/solar.py`, pvlib apparent elevation/azimuth/zenith + day length at campus coords
- [x] Deliverable: `feature_metadata.json` — `artifacts/features/` (+ table at `data/processed/features/`, 2,731,946 × 51, 976 partitions); 24 new tests green

## Phase 6 — XGBoost

- [x] Train/evaluate XGBoost — single-step (1×15-min), config from `configs/{models,training}.yaml` via `src/config.py`; early stopping fired at iter 109/2000; test ALL R² 0.951 / MAE 1.056 vs persistence R² 0.699 / MAE 2.783; MLflow run `xgboost-site-all-h1-v1`
- [x] Deliverables: model artifact (`models/xgboost_site_all_h1_v1.json`, 4.6 MB), metrics (`artifacts/xgboost/metrics.csv`, val+test × ALL+42 sites), predictions (`artifacts/xgboost/predictions_test.parquet`, 409,794 rows), feature importance (`artifacts/xgboost/feature_importance.csv`, gain/weight/cover); + `run_metadata.json` (seed/versions/fingerprints/timing) & `mlruns/`; 14 new tests green (suite total 69)

## Phase 7 — LSTM / GRU

- [x] Sequence/window generation pipeline (PyTorch) — `src/models/sequence_model.py` (`WindowDataset`: per-site windows, 24 h lookback × 13 channels incl. observation mask; train-only scaling; current-step power masked — leak guard D-014 #3 after first run scored R²=1.000 by copying the target)
- [x] LSTM training pipeline: early stopping, LR scheduling, checkpointing, best-model restore, gradient clipping, mixed precision when CUDA available, CPU fallback (PRD §49) — run `lstm-site-all-h1-v1`, test ALL R² 0.948 / MAE 1.140 vs XGBoost 0.951 / 1.056
- [x] GRU training pipeline — run `gru-site-all-h1-v1`, test ALL R² 0.950 / MAE 1.147 / RMSE 2.551 (best RMSE of the three models)
- [x] Deliverables: checkpoints (`models/{lstm,gru}_site_all_h1_v1.pt`), metrics (`artifacts/{lstm,gru}/metrics.csv`, val+test × ALL+42 sites), predictions (`artifacts/{lstm,gru}/predictions_test.parquet`) + `run_metadata.json` + MLflow runs; suite total 86 green

## Phase 8 — Transformer

- [x] Implement ONLY after Phase 7 stable (PRD Rule 8) — `TransformerForecaster` on the shared Phase 7 windows/guard/loop (D-015); run `transformer-site-all-h1-v1`: test ALL R² 0.950 / MAE 1.124 (best deep model; XGBoost 0.951/1.056); no negative-R² sites
- [x] Compare against prior models — `scripts/compare_models.py` → `artifacts/evaluation/{model_comparison.csv,model_comparison.json,evaluation_report.md}` (8 models, verbatim numbers, one protocol)

## Phase 9 — Cross-Site Evaluation

- [x] Chronological split implementation: 70/15/15 train/val/test, temporal order preserved (PRD §11) — formalized pre-existing D-011 implementation (`chronological_split`) with tests; all Phase 5–8 results already used it
- [x] Cross-site split: held-out sites for val/test, dynamically sized from dataset (PRD §12) — `cross_site_split` (D-016): 42 sites → 30/6/6, seeded permutation; full run in `artifacts/cross_site/` — transformer best unseen (MAE 0.764, R² 0.897), XGBoost generalizes worst (R² 0.949→0.795, site 29 R² −6.859); absolute seen/unseen MAE gaps confounded by plant-size mix
- [x] Seen-site vs unseen-site evaluation + dedicated report — `scripts/run_cross_site.py` → `cross_site_metrics.csv` (380 rows), `cross_site_report.md`, `run_metadata.json`
- [x] Automated leakage tests failing CI (PRD §46): future in lags/rolling, test in scaling/feature-selection/tuning, test sites in training — `tests/test_leakage.py` 9 tests; feature-selection/tuning surfaces stubbed until those stages exist

## Phase 10 — Explainability

- [x] SHAP global importance — exact `TreeExplainer` on the Phase 6 XGBoost run over a seeded 20k-row TEST-split sample (D-017): `artifacts/shap/shap_global_importance.csv` + bar/beeswarm PNGs; lag_1 + 1h rolling ≈ 64% of attribution, weather minor; additivity err 6.9e-05
- [x] SHAP local explanations + contribution plots — 4 tagged scenarios (night / clear-noon / ramp / overcast) with waterfall PNGs + `shap_local_contributions.csv`; exposed quantified night failure mode (night MAE 0.157 with lag_1 present vs 5.421 missing — NaN default branches learned from daytime rows)

## Phase 11 — Uncertainty

- [x] Prediction intervals (conformal prediction preferred, PRD §28) — split conformal, absolute residuals (D-018): XGBoost + persistence calibrated on VAL, evaluated on TEST; global + Mondrian regimes `{day,night} × {lag_present,lag_missing}`; `artifacts/uncertainty/` incl. PRD §28-shaped `sample_forecast.json`
- [x] Coverage and interval-width evaluation — all levels ≥ nominal (xgboost 0.916 @ 0.9, ±2.92; persistence 0.922, ±8.20 = 2.8× wider); Mondrian regime widths 0.88 (night+lags) to 26.8 (night+missing lags); caveats: giant sites undercover (0.54–0.59, 9/42 below nominal — scale-aware scores are future work), unclipped bounds go negative in the missing-lag night regime

## Phase 12 — Backend

- [x] FastAPI app under /api/v1: health, dataset, sites, models (PRD §33) — `src/api/{app,store}.py` + `scripts/run_api.py`; registry distinguishes served (xgboost, persistence) vs registered-only (lstm/gru/transformer → 409); D-019
- [x] POST /forecast + POST /forecast/batch (PRD §§33–34) — recursive multi-step (horizon ≤96 = 24 h, batch ≤10): predict→append→rebuild Phase 5 features; weather carried forward; Mondrian conformal bounds per step — `src/api/forecast.py`, D-019
- [x] GET /sites/{site_id}/history (PRD §35) — partition-filtered parquet read (never full table), start/end filters (date-only end → whole day), 15min/1h/1D resampling, 50k-row cap
- [x] GET /models/{model_id}/metrics (PRD §36) — test-split ALL metrics for all 5 models straight from phase artifact CSVs; smoke-verified verbatim vs RESULTS.md; `tests/test_api.py` 17 green (suite 144)

## Phase 13 — Frontend

- [x] React + TypeScript + Vite + Tailwind + shadcn/ui + Recharts scaffold — Vite (rolldown) + Tailwind 4 +
      oxlint; `npm run build` green in 583 ms (single 829 kB bundle / 246 kB gzip); theme provider mounted
      (light/dark via localStorage `unisolar-theme`); D-020 bundled snapshot JSONs for SHAP/quality/sites
- [x] Pages: Dashboard, Forecast, Sites, Model Comparison, Explainability, Data Quality (PRD §38) — all six
      live against FastAPI via Vite proxy; visual verification via Playwright screenshots (6 pages light+dark,
      mobile) with pixel-sampled night shading; is_daylight boolean contract fix D-021

## Phase 14 — Integration

- [x] React → FastAPI → forecasting service → model → processed dataset wiring — full chain live:
      all 5 registered models served (D-022 deep-model recursive path + exported train-split scalers,
      bit-exact checkpoint validation); suite 145 green (18 API); live verify: /models 5×served,
      per-model forecasts OK (transformer 96 steps ≈ 3.7 s CPU), forecast page renders lstm end-to-end
- [x] Forecast latency fix (D-023): timestamp-only features computed once per request, lags/rolling
      incremental per step (`_PowerHistory`, semantics pinned by tests), frontend forecast cache +
      slider debounce — 96-step requests drop from 2.4–3.0 s to 0.09–0.57 s; suite 147 green

## Phase 15 — Testing

- [x] Unit tests: data (timestamps, missing, duplicates, schema), features (lag/rolling/cyclic/solar), split ordering assertions (PRD §45), model I/O dims
- [x] API tests: health, sites, forecast, history, models, metrics
- [x] Leakage tests green in CI

## Phase 16 — Docker

- [x] docker-compose.yml: frontend, backend, ml service (+ Postgres if required)

---

## Post-PRD Enhancement — Train page + richer visuals (D-024/D-025)

- [x] Train page: folder verify (path/upload) → configure → job progress → results;
      job-scoped dirs, one heavy job at a time, served v1 untouched
- [x] Persistence fit-scope fix in train_from_folder (D-011 #3) + pipeline e2e tests
- [x] Marker-based job success (torch exit-9 teardown) + regression tests
- [x] Train API test coverage (verify endpoints, job lifecycle, artifacts, config)
- [x] Export bundles: eda_profiles, missingness_timeline, evaluation_series,
      cross_site_summary (+ unit tests)
- [x] Dashboard: hourly profile + weather↔power heatmap; Quality: monthly gap
      timeline; Comparison: pred-vs-actual explorer, all-runs bars, cross-site card
- [x] First live end-to-end training run verified (see RESULTS.md)

---

## Acceptance criteria (PRD §56)

Tracked at project close. 29/30 done; Random Forest skipped (documented below).

Verified against repo artifacts 2026-09-04 (174 tests green):

- [x] UNISOLAR dataset successfully ingested — Phase 1, `artifacts/data_profile.*`
- [x] Dataset schema automatically inspected — `scripts/inspect_dataset.py`, D-006
- [x] Data-quality report generated — `artifacts/validation_report.md`
- [x] Timestamp integrity validated — Phase 2 (tz D-007, dedup, gap audit)
- [x] Missing data analyzed — Phase 2/3 (missingness structural, D-008)
- [x] Temporal leakage tests implemented — `tests/test_leakage.py` (9, PRD §46)
- [x] Chronological train/validation/test split implemented — D-011 `src/data/splits.py`
- [x] Cross-site split implemented — D-016 `cross_site_split`, 30/6/6
- [x] Persistence baseline implemented — Phase 4, causal t−24h
- [-] Random Forest implemented — **skipped**: never built in this build;
      `configs/models.yaml` has it `enabled: false`, noted on Comparison page
- [x] XGBoost implemented — Phase 6, test MAE 1.056 / R² 0.951
- [x] LSTM implemented — Phase 7, test MAE 1.140 / R² 0.948
- [x] GRU implemented — Phase 7, test MAE 1.147 / R² 0.950
- [x] Transformer implemented — Phase 8, test MAE 1.124 / R² 0.950
- [x] Metrics calculated — MAE/RMSE/R²/nRMSE per model × split × site
- [x] Daylight metrics calculated — daylight_mae/daylight_nrmse in all metrics CSVs
- [x] Model comparison generated — `artifacts/evaluation/` (8 models, one protocol)
- [x] Forecast visualization generated — frontend Forecast page, Playwright-verified
- [x] SHAP implemented — Phase 10, exact TreeExplainer, additivity err 6.9e-05
- [x] Uncertainty estimation implemented — Phase 11 split conformal, coverage ≥ nominal
- [x] Best model selected using validation data — val-ranked table in
      `evaluation_report.md` (xgboost best on val 1.230 AND test 1.056)
- [x] Model artifact saved — `models/` (xgboost json + 3 torch checkpoints)
- [x] FastAPI API implemented — Phase 12, all §33–36 routes
- [x] React dashboard implemented — Phase 13, 6 pages
- [x] Tests implemented — 174 green
- [x] Docker deployment implemented — Phase 16, compose + per-service Dockerfiles
- [x] README completed
- [x] RESULTS.md contains actual results
- [x] No fabricated metrics — PRD Rule 4 held; measured numbers only
- [x] No test-set leakage — D-014 incident fixed + guard tests; leakage suite green
- [x] Reproducible training confirmed — D-024 live run reproduced MAE 1.056
      (`artifacts/train_live_run/summary.json`)
